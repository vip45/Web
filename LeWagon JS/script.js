const musician = {
    fname:  "vipin",
    lname: "yadav"
};

// alert([musician.fname,musician.lname])

const firstname = "Hello";
const secondname = "World";

const both = firstname + secondname;
console.log(both);
alert(both);